# Hidea & Speak — Web

Coming soon. Will be built with Next.js 15 + App Router.

Will share the same Supabase backend as the mobile app — same auth,
same conversations, same storage buckets.

## Planned stack
- Next.js 15 (App Router)
- Tailwind CSS
- Supabase JS client (same as mobile)
- Same `supabase/` schema from the monorepo root
